{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "QuoteRequest",
  "description": "The person a quote is asked for. Matches the shared cris.prs.messaging.Person DTO as Jackson writes it.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Who the quote is for.",
      "minLength": 1
    },
    "age": {
      "type": "integer",
      "description": "Age in whole years; the premium is derived from it.",
      "minimum": 0,
      "maximum": 130
    }
  },
  "required": ["name", "age"]
}
