pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'solace-request-reply'
include 'shared-dto'
include 'solace-library'
include 'server'
include 'client'
